import { Button } from "@/components/ui/button";
import React from "react";

const SetupWebhookButton = () => {
  return (
    <div>
      <Button>Setup Webhook</Button>
    </div>
  );
};

export default SetupWebhookButton;
